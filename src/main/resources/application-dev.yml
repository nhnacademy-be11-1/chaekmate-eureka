server:
  port: 8761

eureka:
  client:
    register-with-eureka: false  # 자기 자신을 등록하지 않음
    fetch-registry: false        # 다른 서버에서 정보 안 가져옴
    service-url:
      defaultZone: http://localhost:8761/eureka/
